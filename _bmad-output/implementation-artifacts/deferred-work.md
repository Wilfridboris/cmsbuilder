# Deferred Work

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-platform-data-model-tenant-isolation.md`
  summary: The idempotency unique-violation (23505) race-recovery branch in mutate.ts insertRecord is untested.
  evidence: The FakeClient never returns a Postgres unique violation, so the de-dupe unit test only exercises the pre-insert lookup path; the concurrent-retry branch (guarded by records_idempotency_key_idx) has zero coverage. Settling it needs a fake that models the partial-unique index and returns 23505, or a concurrent-insert case in the real-Supabase integration suite.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-ai-schema-synthetic-data-generation.md`
  summary: Ephemeral pre-claim anonymous organizations minted per generation session have no TTL/cleanup mechanism.
  evidence: Story 1.4's resolved persistence decision mints a real organizations row per anonymous generation session (reusing records/org_schemas, read via admin client). Unclaimed sessions accumulate orphan orgs indefinitely. Needs a scheduled cleanup (cron) of pre-claim orgs older than a TTL (architecture implies 24h) that were never promoted via org_members at claim — belongs with the cross-org cron/offboarding work (Epic 8) or a dedicated maintenance story.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-hard-fallback-template.md`
  summary: When the hard fallback provisions into a REUSED session org that a prior successful generation already seeded with a same-normalized table_key (e.g. `clients`), read-back merges both row sets into the visible reveal.
  evidence: The Story 1.5 fallback reuses `existingOrgId` from the session cookie and provisions its clients/jobs/invoices template; `listRecords(admin, orgId, "clients")` then returns the prior real-generation rows PLUS the new `fallback-clients-*` rows under one table, showing a mixed dataset. The `idempotencyPrefix` change deliberately prevents key-collision dedup but not row coexistence. Only reachable on same-session real-generation → double-failure with a colliding normalized table_key; the primary single-generation flow is unaffected. A correct fix is non-trivial (Story 1.4's review established that soft-delete-before-reseed collides with the 1.2 partial-unique idempotency index — it needs content-based keys or an index change) and belongs with the same reused-org/ephemeral-org cleanup + claim-time reset work (the claim flow clears synthetic/demo data anyway).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-interactive-demo-dashboard.md`
  summary: The demo dashboard's optimistic-edit validate/coerce logic (`validateAndCoerce`/`toInputString`/`inputModeFor` in `RecordDetail.tsx`) has no automated test.
  evidence: These are pure, deterministic functions unit-testable in the node env with no jsdom (exactly the pattern `formatCell` used), but the story's test task added only `format.test.ts`. An inverted empty-check or a dropped numeric guard (rejecting the frozen matrix's "invalid/empty input → inline message, no commit") would ship undetected. Deferred rather than blocked because the frozen spec deliberately scopes the interactive edit to manual checks (the "no DOM/e2e infra" boundary + 1.3–1.5 precedent) and the logic operates only on throwaway client-side demo data (no DB/API, lost on reload). Settling it needs exporting the helper(s) and a small unit test asserting empty-rejection, non-finite-number rejection, and numeric coercion.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-claim-app-via-magic-link.md`
  summary: `POST /api/claim` has no rate limiting, leaving the magic-link dispatch open to email-bombing / repeated-claim abuse.
  evidence: The endpoint calls `signInWithOtp` (an outbound email) for any email an anonymous caller supplies, gated only by a signed session cookie the caller controls. The whole app currently lacks rate limiting (the `/api/generate` endpoint too) and `src/middleware.ts`'s own comment names IP-based rate limiting as a later-epic seam — so this is a cross-cutting concern not caused by Story 2.1. Settling it needs an IP/email throttle (middleware or per-route) applied across the sensitive endpoints.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-claim-app-via-magic-link.md`
  summary: `finalizeClaim` promotes the session org across four non-transactional writes (membership → slug → clear records → consume token) with no rollback.
  evidence: A mid-sequence failure (e.g. the `records` clear throws after membership + slug are set) leaves a half-promoted org; the callback redirects to `?claim=error` with the token unconsumed. Harm is low — the steps are individually idempotent and a retry of the same (unconsumed) token self-heals, and only the user's own synthetic rows are affected (no data leak). A correct fix wraps the promotion in a single Postgres function/transaction (a new RPC), which is more surface than a patch. Settling it needs that transactional RPC plus a mid-sequence-failure test.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-claim-app-via-magic-link.md`
  summary: `src/middleware.ts` tenant-slug protection (public-path allowlist + `/?auth=required` redirect) has no automated test.
  evidence: The repo has no middleware test harness/precedent, and the `/[slug]` page re-checks `getCurrentUser()` + resolves the org under RLS (the hard isolation gate), so the untested middleware is a UX-only redirect classifier — a dropped public path (e.g. `privacy`) would wrongly bounce unauthenticated visitors, but no data leak. Settling it needs a middleware test that mocks `@supabase/ssr` `createServerClient` to return null/non-null users and asserts the `NextResponse` redirect target for a protected slug vs each public path.
